# Submission Artifacts (Placeholders)

This directory will contain final report documents for Assignment 2.

## Files to Produce

- RollNumber-FullName-Report-A02.docx
- RollNumber-FullName-TestReport-A02.docx

## Generation

Run the doc generation script after installing dependencies:

```powershell
python submission/generate_docs.py --roll 0000 --name "Full Name"
```

Then edit the generated .docx files in a word processor and add screenshots / PCAP evidence.
